# MAD session outputs (these should live in user workspaces, not in the skill repo)
mad_sessions/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build / test scratch
*.tmp

# Internal work product — never commit to the public repo (v0.4 cleanup).
# These live in the sibling Joint/mad-skill-private/ folder locally.
/REVIEW_*.md
/build_notes/
/_review/
/_self_audit/
/_v*_audit/

# Per-example internal prompts; keep audit-trail artifacts but not the
# internal prompts that elicited them.
examples/**/build_notes/*_prompt.txt
examples/**/build_notes/*prompt*.txt
